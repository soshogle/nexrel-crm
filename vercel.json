{
  "buildCommand": "rm -rf node_modules/.prisma && prisma generate && node scripts/vercel-build.js && next build",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
