{
  "buildCommand": "prisma db push --skip-generate && prisma generate && node scripts/vercel-build.js && npm run build",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
